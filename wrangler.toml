name = "letterboxd-top500"
main = "src/index.js"
compatibility_date = "2025-01-01"
# Cloudflare account: Mmdju (workers.dev subdomain: mmdju)
account_id = "525eb5c067d16e8bcb2eab23b20ee7a1"

# LOCAL-ONLY placeholders (valid format, never touch the network in `wrangler dev`).
# Before the first real deploy, create the resources and put the real ids here:
#   npx.cmd wrangler kv namespace create CACHE
#   npx.cmd wrangler d1 create letterboxd-top500-db
#   npx.cmd wrangler d1 migrations apply letterboxd-top500-db --remote
[[kv_namespaces]]
binding = "CACHE"
id = "00000000000000000000000000000000"

[[d1_databases]]
binding = "DB"
database_name = "letterboxd-top500-db"
database_id = "00000000-0000-0000-0000-000000000000"
migrations_dir = "migrations"
